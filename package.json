{
  "name": "gigshield",
  "version": "1.0.0",
  "description": "AI-Powered Parametric Insurance for Gig Workers",
  "scripts": {
    "start": "cd server && npm start",
    "build": "npm run install-all && cd client && npm run build",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "install-all": "npm install --legacy-peer-deps && cd client && npm install --legacy-peer-deps && cd ../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
